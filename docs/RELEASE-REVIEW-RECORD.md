# LifeTrkr stable-release review record

**Review date:** August 24, 2026  
**Candidate commit:** `2bd74eadfbca7d0fa29cf35783762d68b9496591`  
**Repository state:** local `main` = `origin/main`; worktree clean when frozen  
**Application version:** `v0.1.10`  
**Review status:** complete with bounded approval  

## Decision question and scope

Can the frozen candidate be handed to Kieran as a controlled pre-production
release without claiming universal reliability, completed account transfer, or
offline support?

**Decision:** `approve-with-limits` for controlled pre-production handoff.
`defer-for-evidence` for public stable/v1.0 approval.

The approved scope is the client-side React/Vite SPA, local browser persistence,
read-only Google integration when separately configured and tested, deterministic
oracle fallback, and GitHub Pages artifact generated by the recorded workflow.
It does not include a completed ownership transfer, verified Google production
OAuth, offline operation, or universal accessibility/reliability claims.

## Frozen identity and hashes

| Input | SHA-256 / identity |
|---|---|
| Git commit | `2bd74eadfbca7d0fa29cf35783762d68b9496591` |
| `package.json` | `a9e4525ccc379ff008d52a942b61429819ce1798b8a7eb31b01450500c10b28a` |
| `package-lock.json` | `8b863b2a5bf9790c67908b99aaec769b7d3952cdc33d34669b7feb0db986f6c1` |
| `dist/index.html` | `ebf67c1cd8634cd8a3b8b09a237ac4af1bcec72ce54f556433b975cd63186e46` |
| `dist/assets/index-CcIRy9rq.js` | `7631459e2657c090e6d63d58e067f4936e067834542145fbb62ff69c8f868ac2` |
| `dist/assets/index-WnUXXFBk.css` | `dc98179c705667daea47cb1de4fb41ec19e7c64bc83136e96438aa32f51c1534` |

The `dist/404.html` Pages fallback was copied from the candidate index during
artifact validation and matched it byte-for-byte.

## Evidence ledger

| ID | Claim | Tier | Evidence | Consequence if false | Next check |
|---|---|---|---|---|---|
| CLM-01 | Candidate is reproducible from a pushed commit | Confirmed | Git identity/status; `package.json`; `package-lock.json` | Handoff cannot identify what was reviewed | Re-run from a fresh clone |
| CLM-02 | Clean install is portable and does not rewrite the lockfile | Confirmed | `npm ci` with public registry; before/after hash; no internal URL | CI may depend on private infrastructure or drift | Repeat on GitHub Actions |
| CLM-03 | Type/build/a11y source/artifact gates pass | Confirmed | `npm run check`, `npm run check:a11y`, `npm run build`, `scripts/inspect-artifact.mjs` | Broken source or Pages artifact could ship | Preserve green CI run |
| CLM-04 | HashRouter Pages artifact has required local assets and fallback | Confirmed in artifact; runtime provisional | `dist/index.html`, `dist/404.html`, artifact script | Published routes/assets can still be stale or misconfigured | Smoke-test deployed URL and all hash routes |
| CLM-05 | Google access is read-only in source | Confirmed in code; runtime provisional | `SCOPES`, Google client modules, baseline | Consent/runtime behavior could violate the product promise | Disposable-account lifecycle test |
| CLM-06 | Oracle provider key is not shipped to the browser | Confirmed in code/bundle scan | `src/lib/oracle.ts`, env docs, production bundle scan | Provider credential exposure | Repeat scan on every release; inspect worker separately |
| CLM-07 | Local records remain isolated and recoverable across normal reloads | Supported in code; runtime provisional | storage namespace and malformed-read handling | Cross-account leakage or lost edits | Two-account, reload, malformed/quota journey |
| CLM-08 | Accessibility is release-ready | Provisional only | Source heuristic and checklist | Users may encounter inaccessible controls | Complete manual matrix |
| CLM-09 | Stable public release is ready | Deferred | Missing live/manual evidence listed above | Users may rely on unproven behavior | Resolve CLM-04, 05, 07, 08 |

## Independent review comparison

Three reviewers were run independently with separate role prompts but the same
repository snapshot. Their agreement is useful but correlated because they read
the same artifact and were produced in one model environment.

### Evidence reviewer

**Decision:** `defer-for-evidence` for stable approval. Confirmed identity,
portable install, code-level read-only/privacy boundaries, and local artifact
checks. Found no frozen published artifact or live OAuth/manual evidence.

### Outcome reviewer

**Decision:** `defer-for-evidence` for stable approval. The handoff is actionable
as a checklist, but unchecked account transfer and final verification mean the
artifact cannot promise Kieran's end-to-end ownership or user outcome.

### Safety and portability reviewer

**Decision:** `defer-for-evidence` for stable approval. Source-level boundaries
are credible, but silent storage-write failures, third-party runtime behavior,
manual accessibility, and published Pages behavior remain material limits.

**Concordance:** material agreement that code/build readiness is not the same as
stable product evidence. The negotiator adopted the shared limitations rather
than treating agreement as proof.

## Targeted falsification pass

The strongest provisional conclusion was: “the candidate is safe to hand off as a
stable product because the build and source checks pass.” The disruptor tested
these counterexamples conceptually against the frozen evidence:

| Hypothesis | What would falsify the objection | Result |
|---|---|---|
| Pages deploy serves an old artifact or broken hash-route assets | Live URL and every documented route/asset return and render from this commit | Not run; objection survives |
| A second Google account can see the first account's namespace | Disposable two-account switch with before/after storage inventory | Not run; objection survives |
| Full/private storage silently loses a user edit | Forced write failure with visible recovery guidance or explicit safe failure | Not run; objection survives |
| Keyboard/screen-reader/200% zoom behavior fails despite source labels | Completed manual matrix on phone and desktop | Not run; objection survives |

No counterexample was disproven by the available evidence. Development is
reopened for those tests before any public stable/v1.0 claim.

## Negotiator record

The negotiator did not average votes. It accepted the mechanical evidence for a
bounded pre-production handoff, narrowed the release language, and retained the
surviving objections as release conditions.

**Outcome:** `approve-with-limits`

**Decisive evidence:** pushed candidate identity; clean portable install;
zero high-severity production audit findings; type-check, accessibility-source,
production build, artifact inspection, workflow restart, and clean preview.

**Limits:** no offline claim; no completed transfer; no production OAuth claim;
no universal persistence, accessibility, third-party availability, or published
Pages claim until the explicit tests above are recorded.

## Risk ownership and expiry

The risk register is maintained in `docs/HANDOFF.md`. The release decision
expires immediately if the package/lockfile, routes, persistence keys/types,
OAuth scopes, external endpoints, oracle boundary, Pages workflow, or release
commit changes. Kieran owns account and service verification after transfer;
Jamie owns the candidate and evidence record until then.

## Checks not run

Published Pages smoke test, real Google OAuth lifecycle, manual assistive
technology matrix, forced storage quota/private-mode test, and owner-approved
handoff rehearsal were not run. Their absence is intentional and recorded, not
treated as a pass.