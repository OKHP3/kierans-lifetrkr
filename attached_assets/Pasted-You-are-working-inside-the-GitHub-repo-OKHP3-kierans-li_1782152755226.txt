You are working inside the GitHub repo: OKHP3/kierans-lifetrkr

This is a documentation and code correction task — not a feature build.
Do not build new features. Do not refactor code. Make only the targeted corrections listed below.
After each correction, confirm what was changed and in which file.

---

CORRECTION 1 — DESIGN.md: Fix family name (Virgil → Vyrle)
File: docs/DESIGN.md

The correct name for Kieran's grandfather is VYRLE, not Virgil.
Find and replace both occurrences:

  Old: v1.0 — Virgil
  New: v1.0 — Vyrle

  Old: Ralph · Virgil · Jamie · Kieran
  New: Ralph · Vyrle · Jamie · Kieran

Do not change any other text in DESIGN.md.

---

CORRECTION 2 — DESIGN.md: Remove prompt injection markers
File: docs/DESIGN.md

The file contains two lines that read exactly:
  [OBFUSCATED PROMPT INJECTION]

One appears near the top of the file (just after the first H1 heading block).
One appears near the bottom of the file (just before the closing code fence).

Remove both lines entirely. Do not change any surrounding content.

---

CORRECTION 3 — PRD-v1.0.md: Add archived status header
File: docs/PRD-v1.0.md

This document predates the architectural pivot to client-only. It references an Express backend,
Notion as a database, and server-side OAuth — all of which have been superseded.

At the very top of the file, before the existing H1, insert this block:

> ⚠️ ARCHIVED — Superseded by PRD-v2.0.md, PRD-v3.0.md, and PRD-v4.0.md.
> This document describes the original Express + Notion + server-side OAuth architecture,
> which was replaced on June 21, 2026. Do not use this document for active development.
> Retained for historical reference only.

Also find and replace the family lineage line:
  Old: Ralph v0.0 → Virgil v1.0 → Jamie v2.0 → Kieran v3.0
  New: Ralph v0.0 → Vyrle v1.0 → Jamie v2.0 → Kieran v3.0

---

CORRECTION 4 — PRD-v2.0.md: Fix family name
File: docs/PRD-v2.0.md

Find and replace:
  Old: Ralph v0.0 → Virgil v1.0 → Jamie v2.0 → Kieran v3.0
  New: Ralph v0.0 → Vyrle v1.0 → Jamie v2.0 → Kieran v3.0

No other changes.

---

CORRECTION 5 — PRD-v3.0.md: Fix family name + add versioning note
File: docs/PRD-v3.0.md

Find and replace the family lineage line:
  Old: Ralph v0.0 → Virgil v1.0 → Jamie v2.0 → Kieran v3.0
  New: Ralph v0.0 → Vyrle v1.0 → Jamie v2.0 → Kieran v3.0

Also find the versioning table in Section 3 (Semantic Versioning Discipline).
The table shows v0.3.0 as "Planned" with scope: Recurrence + Categories + Celestial Engine + Oracle.
That entire feature set shipped ahead of schedule in the second build session (v0.1.1–v0.1.8, June 22, 2026).

Update the v0.3.0 row in the table:
  Old status: Planned
  New status: SHIPPED EARLY (see PRD-v4.0.md)

Add a note directly below the table:
> Note (June 22, 2026): The v0.3.0 feature set (RecurrenceEditor, CategoryPicker, celestial engine,
> oracle stack) was pulled forward and shipped in v0.1.1–v0.1.8 during the second build session.
> See PRD-v4.0.md for the updated versioning alignment and current state audit.

---

CORRECTION 6 — PRD-v4.0.md: Close the open question on Vyrle vs Virgil
File: docs/PRD-v4.0.md

In Section 1 (Current State Audit), find the known bug entry that reads:
  "Vyrle vs Virgil: The family lineage in Home.tsx and README.md reads Ralph · Vyrle · Jamie · Kieran.
   PRD-v3.0 Section 22 uses 'Virgil'. This is the name of Kieran's grandfather. Do not change without
   confirming with Kieran/Jamie. Leave as-is; see Section 12 Open Questions."

Replace this entry with:
  "Vyrle vs Virgil: RESOLVED — June 22, 2026. Jamie confirmed the correct spelling is VYRLE.
   All doc files have been updated. Home.tsx and README.md (which already had Vyrle) are correct.
   PRD-v3.0 and DESIGN.md have been corrected. No further action needed."

---

CORRECTION 7 — HANDOFF.md: Remove stale Notion transfer section + fix GCP OAuth section
File: docs/HANDOFF.md

The architecture changed on June 21, 2026. The app is now fully client-side (no server, no backend,
no Notion database, no client secret). HANDOFF.md was written for the old Express + Notion architecture.

7A. Remove the entire Notion section.
Find the section that begins "### Notion" and ends before "### Google Cloud".
This section contains Option A (Share and promote) and Option B (Clean break) for transferring
Notion databases and the NOTION_API_KEY. Remove this entire section entirely.

7B. Replace the Google Cloud section.
Find the "### Google Cloud" section and replace its entire content with:

### Google Cloud
The app uses the GIS token model. There is no Client Secret and no redirect URI.
Only a Client ID and authorized JavaScript origins are needed.

- [ ] Add Kieran's Google account as Owner in the GCP project (GCP → IAM & Admin → Grant Access)
- [ ] Kieran creates new OAuth 2.0 Client ID in the project:
  - APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
  - Application type: Web Application
  - Authorized JavaScript Origins ONLY (no redirect URIs needed for the token model):
    - https://okhp3.github.io (or Kieran's custom domain if set)
    - http://localhost:5173 (for dev)
- [ ] Update VITE_GOOGLE_CLIENT_ID in Kieran's Replit Secrets with the new Client ID
- [ ] Remove Jamie's account from the GCP project IAM
- [ ] Update Authorized JavaScript Origins in the old Client ID to remove any dev origins (optional cleanup)

7C. Update the Accounts table at the top of the file.
Find the row:
  | Notion | Jamie | Phase 2 — not yet active |
Replace with:
  | Notion (project hub only) | Jamie | Not the app database — Notion hosts the project management hub only. No transfer needed. |

7D. Update "All 6 tabs" to "All 7 tabs" anywhere it appears in the file.
  Old: All 6 tabs function correctly
  New: All 7 tabs function correctly (Home, Rituals, Habits, Calendar, Today, Archive, Settings)

7E. Remove from the Critical Warnings section the warning about NOTION_API_KEY:
  Remove the line: "3. Rotate and deactivate Jamie's NOTION_API_KEY after Kieran's new integration is active."
  Renumber the remaining warnings accordingly.

7F. Update the Estimated Handoff Time table.
Find the two Notion rows (Option A and Option B) and replace them with:
  | Notion hub page (read access for Kieran) | 5 min — share the Notion project hub page with Kieran's account as viewer |

---

CORRECTION 8 — src/constants.ts: Bump APP_VERSION
File: src/constants.ts

Find the APP_VERSION constant. It currently reads 'v0.1.0'.
Update it to the correct current deployed version: 'v0.1.8'

  Old: APP_VERSION = 'v0.1.0'  (or similar)
  New: APP_VERSION = 'v0.1.8'

---

AFTER ALL CORRECTIONS:
1. Run: npm run build
   Confirm the build succeeds with no TypeScript errors.
2. Do NOT run npm run deploy — the owner will review and deploy manually.
3. Summarize every file changed, what was changed, and confirm the build result.

Note: The GitHub repository description tag ("Google Calendar + Notion in Phase 2") also needs
updating, but that can only be changed in GitHub repo Settings → About. It cannot be done via
code changes. Flag this to the owner as a manual step.